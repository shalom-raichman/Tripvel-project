import { Router } from 'express'

const router = Router()

router.get('/', () => {})

router.get('/deshboard', () => {})

router.post('/', () => {})

router.patch('/:id', () => {})

export default router
